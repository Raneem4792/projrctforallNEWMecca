# إعداد نظام إدارة المستشفيات

## 🚀 خطوات التطبيق

### 1. تحديث قاعدة البيانات

قم بتشغيل الملف التالي لتحديث جدول المستشفيات:

```bash
cd backend
node run-hospitals-update.js
```

أو يمكنك تشغيل SQL مباشرة:

```bash
mysql -u root -p your_database_name < sql/update-hospitals-table.sql
```

### 2. تشغيل الخادم

```bash
npm start
# أو
npm run dev
```

### 3. اختبار API

```bash
# جلب قائمة المستشفيات
curl http://localhost:3001/api/hospitals

# جلب مستشفى واحد
curl http://localhost:3001/api/hospitals/1

# إضافة مستشفى جديد
curl -X POST http://localhost:3001/api/hospitals \
  -H "Content-Type: application/json" \
  -d '{
    "NameAr": "مستشفى تجريبي",
    "NameEn": "Test Hospital",
    "Code": "TEST",
    "CityAr": "مكة",
    "CityEn": "Makkah",
    "RegionAr": "الغربية",
    "RegionEn": "Western",
    "IsActive": 1,
    "SortOrder": 100
  }'
```

## 📋 API Endpoints

### المستشفيات
- `GET /api/hospitals` - جلب قائمة المستشفيات
- `GET /api/hospitals/:id` - جلب مستشفى واحد
- `POST /api/hospitals` - إضافة مستشفى جديد
- `PUT /api/hospitals/:id` - تحديث مستشفى
- `DELETE /api/hospitals/:id` - حذف مستشفى

## 🗃️ بنية الجدول المحدثة

```sql
CREATE TABLE hospitals (
  HospitalID INT PRIMARY KEY AUTO_INCREMENT,
  NameAr VARCHAR(100) NOT NULL,
  NameEn VARCHAR(100) NOT NULL,
  Code VARCHAR(20) UNIQUE NOT NULL,
  CityAr VARCHAR(100),
  CityEn VARCHAR(100),
  RegionAr VARCHAR(100),
  RegionEn VARCHAR(100),
  IsActive TINYINT(1) NOT NULL DEFAULT 1,
  SortOrder SMALLINT UNSIGNED NOT NULL DEFAULT 100,
  CreatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## 🎨 واجهة المستخدم

الصفحة `admin-hospitals.html` الآن مربوطة بالـ API وتدعم:

- ✅ عرض المستشفيات من قاعدة البيانات
- ✅ حذف المستشفيات
- ✅ عرض حالة التفعيل (مفعّل/غير مفعّل)
- ✅ عرض المدينة والمنطقة
- ✅ ترتيب حسب SortOrder

## 🔧 الميزات المضافة

1. **أعمدة جديدة:**
   - `CityAr`, `CityEn` - المدينة بالعربية والإنجليزية
   - `RegionAr`, `RegionEn` - المنطقة بالعربية والإنجليزية
   - `IsActive` - حالة التفعيل
   - `SortOrder` - ترتيب العرض

2. **فهارس للأداء:**
   - فهرس فريد لكود المستشفى
   - فهارس للبحث والترتيب

3. **حماية البيانات:**
   - منع حذف المستشفيات المرتبطة بشكاوى
   - التحقق من عدم تكرار الكود

## 📊 البيانات التجريبية

تم إدراج 6 مستشفيات تجريبية:
- مستشفى الملك عبدالعزيز (KAH)
- مستشفى حراء العام (HRH)
- مستشفى النور التخصصي (NRH) - غير مفعّل
- مستشفى الولادة والأطفال (WCH) - غير مفعّل
- مستشفى الهدى العام (HDH)
- مركز العزيزية (AZC)
