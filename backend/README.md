# نظام البلاغات - الباك-إند

## 🚀 التشغيل السريع

### 1. تثبيت المتطلبات
```bash
cd backend
npm install
```

### 2. إعداد قاعدة البيانات
- تأكد من تشغيل MySQL
- قم بتحديث ملف `.env` بالبيانات الصحيحة:
```env
DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=your_password
DB_NAME=your_db
```

### 3. تشغيل الخادم
```bash
# وضع التطوير (مع إعادة التشغيل التلقائي)
npm run dev

# أو وضع الإنتاج
npm start
```

## 📋 API Endpoints

### المصادقة
- `POST /api/auth/signup` - تسجيل مستخدم جديد (يدعم الأقسام المتدرجة)
- `POST /api/auth/login` - تسجيل دخول (اسم مستخدم أو بريد إلكتروني)

### البيانات المرجعية
- `GET /api/lookups/departments` - قائمة الأقسام النشطة
- `GET /api/lookups/hospitals` - قائمة المستشفيات النشطة
- `GET /api/lookups/categories` - فئات البلاغات

### الأقسام المتقدمة
- `GET /api/lookups/departments/tree` - شجرة الأقسام (مع فرز حسب العمق)
- `GET /api/lookups/departments/mains` - الأقسام الرئيسية فقط
- `GET /api/lookups/departments/by-parent/:parentId` - الأقسام الفرعية لقسم معين

### إدارة الأقسام (Admin)
- `POST /api/admin/departments` - إضافة قسم جديد
- `PUT /api/admin/departments/:id` - تحديث قسم موجود
- `DELETE /api/admin/departments/:id` - حذف قسم (تعطيل)
- `GET /api/admin/departments/:id` - تفاصيل قسم معين

### الصحة
- `GET /api/health` - اختبار صحة الخادم

## 🔧 إعدادات مهمة

### ملف .env
```env
PORT=3001
DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=your_password
DB_NAME=your_db
JWT_SECRET=supersecret_jwt_key_2025_complaints_system
JWT_EXPIRES=7d
CORS_ORIGIN=http://127.0.0.1:5500
```

### ربط الواجهة الأمامية
1. أضف ملف `signup-integration.js` لصفحة التسجيل
2. تأكد من تشغيل الباك-إند على المنفذ 3001
3. استخدم المتغير `API_BASE = 'http://localhost:3001/api'`

## 🛡️ الأمان

- تشفير كلمات المرور بـ bcrypt
- حماية من CORS
- تحديد معدل الطلبات (Rate Limiting)
- Helmet للأمان الأساسي
- JWT للمصادقة

## 📊 قاعدة البيانات

المشروع يتطلب جداول:
- `users` - المستخدمين
- `departments` - الأقسام
- `hospitals` - المستشفيات
- `roles` - الأدوار
- `user_permission_flags` - صلاحيات المستخدمين

## 🔍 استكشاف الأخطاء

1. تأكد من تشغيل MySQL
2. تحقق من إعدادات `.env`
3. تأكد من وجود البيانات في قاعدة البيانات
4. تحقق من console للرسائل

## 📝 ملاحظات

- جميع الرسائل باللغة العربية
- معالجة شاملة للأخطاء
- دعم UTF-8 للنصوص العربية
- تسجيل مفصل للعمليات
