# إعداد CORS لحل مشكلة الوصول للـ API

## 🔧 المشكلة
الواجهة تعمل على المنفذ 5500 والـ API على المنفذ 3001، مما يسبب مشكلة CORS.

## ✅ الحل

### 1. إعداد ملف .env
أنشئ ملف `.env` في مجلد `backend` مع المحتوى التالي:

```env
# إعدادات قاعدة البيانات
DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=your_password
DB_NAME=your_database_name

# إعدادات الخادم
PORT=3001

# إعدادات JWT
JWT_SECRET=supersecret_jwt_key_2025_complaints_system
JWT_EXPIRES=7d

# إعدادات CORS - للسماح بالوصول من الواجهة
CORS_ORIGIN=http://localhost:5500,http://127.0.0.1:5500,http://localhost:3000,http://127.0.0.1:3000
```

### 2. أو تعديل app.js مباشرة
إذا لم تستطع إنشاء ملف .env، عدّل app.js:

```javascript
app.use(cors({
  origin: [
    'http://localhost:5500',
    'http://127.0.0.1:5500',
    'http://localhost:3000',
    'http://127.0.0.1:3000'
  ],
  credentials: true
}));
```

## 🧪 اختبار الحل

### 1. تشغيل الخادم
```bash
cd backend
npm start
```

يجب أن ترى:
```
🚀 API يعمل على http://localhost:3001
```

### 2. اختبار API مباشرة
افتح في المتصفح: `http://localhost:3001/api/hospitals`

يجب أن ترى JSON مع المستشفيات.

### 3. اختبار الصفحة
افتح: `http://localhost:5500/NewProjectMecca/public/admin/admin-hospitals.html`

يجب أن ترى المستشفيات تظهر بدون أخطاء CORS.

## 🔍 استكشاف الأخطاء

### إذا ظهر خطأ CORS:
1. تأكد من أن الخادم يعمل على المنفذ 3001
2. تأكد من إعدادات CORS_ORIGIN
3. تأكد من أن الصفحة تستخدم `API_BASE` الصحيح

### إذا ظهر خطأ 404:
1. تأكد من أن route المستشفيات مسجل في app.js
2. تأكد من أن جدول hospitals موجود
3. شغّل `node run-hospitals-update.js` لتحديث قاعدة البيانات
