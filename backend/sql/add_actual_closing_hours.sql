-- =====================================================
-- إضافة عمود ActualClosingHours لحساب وقت الإغلاق الفعلي
-- =====================================================
-- هذا العمود يحسب الوقت الفعلي من CreatedAt إلى لحظة الإغلاق
-- يختلف عن ProcessingDurationHours (الذي هو SLA مستهدف)

-- ✅ تطبيق على كل قاعدة بيانات مستشفى
-- استبدل hosp_XXX باسم قاعدة بيانات المستشفى
-- أو استخدم في سكريبت يطبق على جميع قواعد البيانات

-- إضافة العمود
ALTER TABLE complaints 
ADD COLUMN IF NOT EXISTS ActualClosingHours INT NULL 
COMMENT 'الوقت الفعلي للإغلاق بالساعات (من CreatedAt إلى لحظة الإغلاق)' 
AFTER ProcessingDurationHours;

-- =====================================================
-- ملاحظة: سيتم تحديث هذا العمود تلقائياً عند الإغلاق
-- من خلال complaintStatusesController.js
-- =====================================================

