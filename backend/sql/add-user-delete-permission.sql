-- =====================================================
-- إضافة صلاحية حذف المستخدم
-- =====================================================
-- هذا السكريبت يضيف صلاحية "حذف المستخدم"
-- للتحكم في من يستطيع حذف المستخدمين
-- =====================================================

USE makkah_central;

-- إضافة الصلاحية في جدول permissions المركزي
INSERT IGNORE INTO permissions (PermissionKey, NameAr, NameEn, Category, Description)
VALUES (
  'HOSPITAL_USER_DELETE',
  'حذف المستخدم',
  'Delete User',
  'hospital',
  'صلاحية حذف المستخدمين من المستشفى'
);

-- =====================================================
-- ملاحظات:
-- =====================================================
-- 1. هذه الصلاحية تُحفظ في جدول user_permissions لكل مستشفى
-- 2. مدير التجمع (Central Admin) له الصلاحية تلقائياً
-- 3. باقي المستخدمين يحتاجون منحها من صفحة الصلاحيات
-- 4. تتحكم في إظهار/إخفاء زر "🗑️ حذف" لكل مستخدم
-- 5. منفصلة عن صلاحيتي الإضافة والتعديل
-- 
-- الصلاحيات الثلاث للمستخدمين:
-- - HOSPITAL_USER_CREATE  → إضافة مستخدم جديد
-- - HOSPITAL_USER_EDIT    → تعديل بيانات المستخدم
-- - HOSPITAL_USER_DELETE  → حذف المستخدم
-- 
-- مثال لمنح الصلاحية يدوياً:
-- 
-- USE hosp_ABC;  -- قاعدة المستشفى
-- INSERT INTO user_permissions (UserID, HospitalID, PermissionKey)
-- VALUES (123, 1, 'HOSPITAL_USER_DELETE');
-- 
-- =====================================================

-- التحقق من إضافة الصلاحية
SELECT PermissionKey, NameAr, Category 
FROM permissions 
WHERE PermissionKey = 'HOSPITAL_USER_DELETE';

