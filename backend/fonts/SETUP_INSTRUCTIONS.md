# 📥 تعليمات تحميل الخطوط العربية

## الخطوات السريعة:

### 1. تحميل خط Tajawal

**الطريقة الأسهل:**
1. افتح: https://fonts.google.com/specimen/Tajawal
2. اضغط على زر **"Download family"** (أسفل الصفحة)
3. استخرج الملف المضغوط (ZIP)
4. انسخ الملفين التاليين إلى مجلد `backend/fonts/`:
   - `Tajawal-Regular.ttf`
   - `Tajawal-Bold.ttf`

### 2. التحقق

بعد تحميل الملفات، يجب أن يكون لديك:

```
backend/fonts/
  ├── Tajawal-Regular.ttf
  ├── Tajawal-Bold.ttf
  ├── README.md
  └── SETUP_INSTRUCTIONS.md (هذا الملف)
```

### 3. إعادة تشغيل السيرفر

بعد إضافة الخطوط، أعد تشغيل السيرفر:

```bash
cd backend
node app.js
```

**يجب أن ترى في التيرمنال:**
```
✅ [PDF] تم تسجيل الخطوط العربية بنجاح
```

**إذا رأيت:**
```
⚠️ [PDF] لم يتم العثور على الخطوط العربية
```
← تأكد من أن الملفات موجودة في المسار الصحيح.

---

## 🔄 بدائل

إذا لم تستطع تحميل Tajawal، يمكنك استخدام أي خط عربي TTF آخر:

- **Amiri**: https://fonts.google.com/specimen/Amiri
- **Cairo**: https://fonts.google.com/specimen/Cairo
- **Noto Sans Arabic**: https://fonts.google.com/specimen/Noto+Sans+Arabic

**ملاحظة:** إذا غيرت اسم الخط، يجب تعديل الأسماء في `backend/controllers/reportsController.js`:
```javascript
const AR_FONT_REGULAR = path.join(__dirname, '..', 'fonts', 'اسم-الخط-Regular.ttf');
const AR_FONT_BOLD = path.join(__dirname, '..', 'fonts', 'اسم-الخط-Bold.ttf');
```

---

## ✅ اختبار

بعد تحميل الخطوط وإعادة تشغيل السيرفر:

1. افتح صفحة التقارير
2. اضغط زر **PDF** لتقرير "ملخّص التجمع"
3. افتح الملف المُحمّل
4. تأكد من أن النصوص العربية تظهر بشكل صحيح ✅

