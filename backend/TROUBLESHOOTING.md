# 🔧 حل المشاكل - نظام إدارة المستشفيات

## 🚨 المشاكل الشائعة والحلول

### 1️⃣ خطأ 404 على /api/hospitals

**المشكلة:** الصفحة لا تستطيع الوصول للـ API

**الحل:**
```javascript
// في admin-hospitals.html
const API_BASE = 
  location.hostname === 'localhost' || location.hostname === '127.0.0.1'
    ? 'http://localhost:3001'
    : '';

// استخدام API_BASE في جميع الطلبات
fetch(`${API_BASE}/api/hospitals`)
```

### 2️⃣ خطأ CORS

**المشكلة:** المتصفح يمنع الطلبات بين المنافذ المختلفة

**الحل:**
1. أنشئ ملف `.env` في مجلد `backend`:
```env
CORS_ORIGIN=http://localhost:5500,http://127.0.0.1:5500
```

2. أو عدّل `app.js`:
```javascript
app.use(cors({
  origin: ['http://localhost:5500', 'http://127.0.0.1:5500'],
  credentials: true
}));
```

### 3️⃣ خطأ قاعدة البيانات

**المشكلة:** جدول hospitals غير موجود أو ناقص الأعمدة

**الحل:**
```bash
cd backend
node run-hospitals-update.js
```

### 4️⃣ خطأ favicon 404

**المشكلة:** المتصفح يبحث عن أيقونة غير موجودة

**الحل:** تم إضافة favicon في الصفحة:
```html
<link rel="icon" href="../assets/img/logo.png" type="image/png">
```

## 🧪 اختبار شامل

### 1. اختبار قاعدة البيانات
```bash
cd backend
node test-hospitals-api.js
```

### 2. اختبار الخادم
```bash
cd backend
npm start
```
يجب أن ترى: `🚀 API يعمل على http://localhost:3001`

### 3. اختبار API مباشرة
افتح: `http://localhost:3001/api/hospitals`
يجب أن ترى JSON مع المستشفيات.

### 4. اختبار الصفحة
افتح: `http://localhost:5500/NewProjectMecca/public/admin/admin-hospitals.html`
يجب أن ترى المستشفيات بدون أخطاء.

## 📋 قائمة التحقق السريعة

- [ ] الخادم يعمل على المنفذ 3001
- [ ] قاعدة البيانات محدثة (node run-hospitals-update.js)
- [ ] ملف .env يحتوي على CORS_ORIGIN
- [ ] الصفحة تستخدم API_BASE الصحيح
- [ ] لا توجد أخطاء في console المتصفح

## 🆘 إذا لم تعمل

1. **تحقق من console المتصفح** للأخطاء
2. **تحقق من console الخادم** للأخطاء
3. **شغّل الاختبار:** `node test-hospitals-api.js`
4. **تحقق من إعدادات قاعدة البيانات** في .env
5. **تأكد من تشغيل MySQL**

## 📞 الدعم

إذا استمرت المشاكل، تحقق من:
- إعدادات قاعدة البيانات
- إعدادات CORS
- وجود جدول hospitals
- صحة API routes
