# 🏥 تشغيل سريع - نظام إدارة المستشفيات

## خطوات سريعة (5 دقائق)

### 1️⃣ تحديث قاعدة البيانات
```bash
cd backend
node run-hospitals-update.js
```

### 2️⃣ تشغيل الخادم
```bash
npm start
```

### 3️⃣ فتح الصفحة
افتح: `NewProjectMecca/public/admin/admin-hospitals.html`

## ✅ ما تم إنجازه

- ✅ إضافة أعمدة جديدة لجدول المستشفيات
- ✅ إنشاء API كامل للمستشفيات (CRUD)
- ✅ ربط الصفحة بقاعدة البيانات
- ✅ إضافة بيانات تجريبية
- ✅ دعم عرض المدينة والمنطقة
- ✅ دعم حالة التفعيل/الإلغاء
- ✅ حماية من حذف المستشفيات المرتبطة بشكاوى

## 🔗 API Endpoints

- `GET /api/hospitals` - قائمة المستشفيات
- `POST /api/hospitals` - إضافة مستشفى
- `PUT /api/hospitals/:id` - تحديث مستشفى  
- `DELETE /api/hospitals/:id` - حذف مستشفى

## 🎯 النتيجة

الصفحة الآن تعرض المستشفيات من قاعدة البيانات مع:
- الاسم بالعربية
- الكود
- المدينة - المنطقة
- حالة التفعيل
- أزرار التعديل والحذف
