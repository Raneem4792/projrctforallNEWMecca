# 🔍 تشخيص مشكلة سجل البلاغات

## 🎯 **المشكلة:**
الصفحة تظهر "لا توجد بلاغات مطابقة للبحث" رغم إرسال التوكن بشكل صحيح.

## 🔍 **التحليل من console المتصفح:**
- ✅ التوكن يُرسل: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1aWQiOjMsI...`
- ✅ الطلب يذهب للسيرفر: `http://localhost:3001/api/complaints/history?page=1&pageSize=9`
- ✅ المستخدم مسجل دخول بنجاح
- ❌ النتائج فارغة (0 بلاغات)

## 🧪 **خطوات التشخيص:**

### **1. فحص السيرفر:**
```bash
# تأكد من أن السيرفر يعمل
cd backend
npm start
# أو
node app.js
```

### **2. فحص console السيرفر:**
يجب أن ترى رسائل مثل:
```
🔐 [AUTH] التحقق من التوكن: { hasHeader: true, hasToken: true }
✅ [AUTH] التوكن صالح: { userId: 3, roleId: 2, hospitalId: 11 }
📋 [HISTORY] البحث | hasUser: true | hospitalId: 11 | queryHospitalId: none | page: 1
📋 [HISTORY] الفلاتر: { name: '', mobile: '', file: '', ticket: '', status: 'ALL', from: '', to: '' }
📋 [HISTORY] الباراميترات: { page: '1', pageSize: '9' }
📋 [HISTORY] نطاق المستشفى: { where: ' AND c.HospitalID = ? ', params: [11] }
📋 [HISTORY] استعلام المركزية: SELECT c.ComplaintID AS id, c.TicketNumber AS ticket...
📋 [HISTORY] معاملات المركزية: [11, 9, 0]
📋 [HISTORY] نتائج المركزية: { rowsCount: 0, total: 0, kpis: { openCount: 0, closedCount: 0, criticalCount: 0 } }
🔄 البحث في قاعدة المستشفى 11 كبديل احتياطي
```

### **3. إذا لم ترى رسائل السيرفر:**
- السيرفر لا يعمل
- أو هناك خطأ في التوكن
- أو الطلب لا يصل للسيرفر

### **4. إذا رأيت رسائل السيرفر لكن النتائج فارغة:**
- لا توجد بيانات في القاعدة المركزية
- لا توجد بيانات في قاعدة المستشفى
- مشكلة في الاستعلام

## 🔧 **الحلول:**

### **الحل 1: السيرفر لا يعمل**
```bash
cd backend
npm start
# تأكد من ظهور: "Server running on port 3001"
```

### **الحل 2: خطأ في التوكن**
```javascript
// في console المتصفح:
const token = localStorage.getItem('token');
console.log('التوكن:', token);

// فحص محتوى التوكن:
const payload = JSON.parse(atob(token.split('.')[1]));
console.log('محتوى التوكن:', payload);
console.log('hospitalId:', payload.hospitalId);
```

### **الحل 3: لا توجد بيانات**
```bash
# فحص البيانات في القاعدة
cd backend
node test-history-data.js
```

### **الحل 4: مشكلة في الاستعلام**
```bash
# فحص الاستعلام مباشرة
cd backend
node -e "
import { centralDb } from './config/db.js';
const [rows] = await centralDb.query('SELECT COUNT(*) as count FROM complaints WHERE HospitalID = 11');
console.log('بلاغات مستشفى 11:', rows[0].count);
"
```

## 🎯 **الخطوات السريعة:**

### **1. فحص السيرفر:**
```bash
# في terminal منفصل:
cd backend
npm start
```

### **2. فحص console السيرفر:**
- افتح صفحة سجل البلاغات
- راقب console السيرفر
- ابحث عن رسائل `[HISTORY]`

### **3. إذا لم تظهر رسائل:**
- السيرفر لا يعمل
- أو الطلب لا يصل

### **4. إذا ظهرت رسائل لكن النتائج فارغة:**
- لا توجد بيانات
- أو مشكلة في الاستعلام

## 🚨 **المشاكل الشائعة:**

### **1. السيرفر لا يعمل:**
```
Error: connect ECONNREFUSED localhost:3001
```
**الحل:** شغل السيرفر

### **2. خطأ في التوكن:**
```
❌ [AUTH] التوكن غير صالح: jwt expired
```
**الحل:** سجل دخول مرة أخرى

### **3. لا توجد بيانات:**
```
📋 [HISTORY] نتائج المركزية: { rowsCount: 0, total: 0 }
```
**الحل:** أضف بيانات تجريبية أو تحقق من المزامنة

### **4. مشكلة في الاتصال:**
```
❌ خطأ في الاتصال: fetch failed
```
**الحل:** تأكد من أن السيرفر يعمل على المنفذ 3001

---

## 🎯 **الخلاصة:**

**المشكلة الأكثر احتمالاً:** السيرفر لا يعمل أو لا توجد بيانات في القاعدة.

**الحل السريع:** 
1. شغل السيرفر
2. راقب console السيرفر
3. تحقق من وجود البيانات
